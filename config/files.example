# Secrets whose value is a whole file (OAuth credentials, service-account JSON).
# Same format: one name per line, filled in by `mcp-secrets set-file NAME FILE`.
# Unlike the names in `vars`, these are never exported into the environment —
# a launcher materializes them into a temporary file.
#
# GOOGLE_CREDENTIALS_JSON
