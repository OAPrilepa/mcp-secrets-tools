# One environment-variable name per line — names only, never values.
# The values live in the keychain: `mcp-secrets set MY_API_TOKEN` stores one and
# appends the name here for you.
#
# MY_API_TOKEN
# SOME_SERVICE_COOKIE
